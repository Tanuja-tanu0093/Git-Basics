🚀 STEP-BY-STEP: Create Files & Push to GitHub
🟢 STEP 1: Create a project folder (LOCAL)

Open Git Bash.

mkdir my-github-practice
cd my-github-practice


✔ This folder will contain your files.

Check:

pwd

🟢 STEP 2: Initialize Git in that folder
git init


✔ Output should be:

Initialized empty Git repository


🧠 Now Git is watching this folder.

🟢 STEP 3: Create your own files

Create a file:

touch hello.txt


Open hello.txt (Notepad) and write:

Hello GitHub
This is my first push


Save it.

🟢 STEP 4: Check Git status
git status


You should see:

Untracked files:
  hello.txt

🟢 STEP 5: Add files to Git (STAGE)
git add hello.txt


(or all files)

git add .


Check:

git status


✔ Should show file in staged area.

🟢 STEP 6: Commit your changes (SAVE POINT)
git commit -m "Added hello file"


✔ Output:

1 file changed


🧠 Commit = “this version is correct”

🟢 STEP 7: Create a GitHub repository (ONLINE)

Open 👉 https://github.com

Click New Repository

Repository name:

my-github-practice


❌ Do NOT add README

Click Create repository

⚠️ Leave the page open (important).

🟢 STEP 8: Connect LOCAL repo to GitHub

GitHub will show commands.
Copy only these two lines (example):

git remote add origin https://github.com/your-username/my-github-practice.git
git branch -M main


👉 Paste them into Git Bash and press Enter.

🟢 STEP 9: Push your code to GitHub 🎉
git push -u origin main


Enter:

GitHub username

GitHub password (or token)

✔ If successful, no error.

🟢 STEP 10: VERIFY 🎯

Refresh your GitHub repository page.

You should see:

hello.txt

Your content inside the file

🎉 CONGRATULATIONS! FIRST PUSH DONE 🎉

🧠 REMEMBER THIS FLOW (VERY IMPORTANT)
Create files
↓
git add
↓
git commit
↓
git push


------------------**********************************---------------------------------------
Git Commands for Renaming Files & Updating GitHub
1️⃣ Rename a file locally
mv oldfilename.txt newfilename.txt


mv = move/rename

Example:

mv hello.txt greeting.txt


✅ This renames the file in your local folder.

2️⃣ Check Git status
git status


Shows something like:

renamed: hello.txt -> greeting.txt


Confirms Git tracked the rename

3️⃣ Stage the change
git add .


Adds the renamed file to staging area

Prepares it for commit

4️⃣ Commit the change
git commit -m "Renamed hello.txt to greeting.txt"


Saves a new version of your project

Commit message explains what changed

5️⃣ Push the change to GitHub
git push


Updates GitHub repository with renamed file

GitHub shows new file name

History still tracks the old name

6️⃣ Check commit history (optional)
git log --name-status


Shows R oldfilename.txt newfilename.txt

R = Rename

Lets you see old → new file name

7️⃣ Check content changes (optional)
git diff HEAD~1 HEAD




✅ Quick Summary
Task	                   Command
Rename locally	    ------ mv old.txt new.txt
Stage the change    ------git add .
                     ------Commit git commit -m "Renamed file"
Push to GitHub	       -----git push
Check rename history   ------ git log --name-status
See content changes	-------git diff HEAD~1 HEAD


